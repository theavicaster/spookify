jwt:
  secret: ${JWT_SECRET}
spring:
  data:
    jpa:
      repositories:
        bootstrap-mode: default
  datasource:
    url: jdbc:h2:mem:testdb
  h2:
    console:
      enabled: true
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss